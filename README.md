# config-generator-
VLESS

> یک اسکنر پرسرعت و چندنخی برای پیدا کردن آی‌پی‌های سالم Cloudflare به همراه تولید خودکار کانفیگ VLESS و لینک سابسکریپشن آماده استفاده.

---

## ✨ ویژگی‌ها

* ⚡ اسکن بسیار سریع با چند ترد
* 🔍 تشخیص خودکار پورت فعال
* 🤝 بررسی هوشمند هندشیک Cloudflare
* 📶 اندازه‌گیری پینگ دو مرحله‌ای
* 🚀 تست واقعی سرعت دانلود و آپلود
* 🧠 انتخاب هوشمند بهترین IP
* 📦 تولید خودکار کانفیگ VLESS
* 🔗 خروجی سابسکریپشن base64
* 🧩 پشتیبانی از چند DNS مختلف
* ⛔ امکان توقف اسکن در هر لحظه (Ctrl + M)
* 🎨 خروجی رنگی در ترمینال
* 🗂 پاکسازی خودکار پوشه خروجی
* 🔒 تشخیص خودکار TLS و غیر TLS
* 🧪 بهینه‌سازی Fragment برای اتصال پایدارتر

---

## 🖥️ پیش‌نیازها

* Python **3.9 یا بالاتر**
* ویندوز (پیشنهادی)
* دسترسی به اینترنت

### نصب کتابخانه‌ها

```bash
pip install requests colorama urllib3
```

---

## ⚙️ تنظیمات اولیه

قبل از اجرا، این مقادیر را در فایل ویرایش کنید:

```python
WORKER_HOST = "your-worker.workers.dev"
WS_PATH = "/your-websocket-path"
USER_UUID = "your-uuid"
```

---

## 🔧 بخش‌های قابل شخصی‌سازی

| متغیر              | توضیح                    |
| ------------------ | ------------------------ |
| `HTTP_PORTS`       | پورت‌هایی که تست می‌شوند |
| `CLOUDFLARE_CIDRS` | رنج‌های آی‌پی Cloudflare |
| `SAMPLES_PER_CIDR` | تعداد نمونه از هر رنج    |
| `MAX_WORKERS`      | تعداد تردهای همزمان      |
| `TIMEOUT`          | زمان انتظار اتصال        |
| `DNS_SERVERS`      | لیست DNSهای تست          |

---

## 🚀 نحوه اجرا

```bash
python scanner.py
```

---

## 🎯 حالت‌های اجرا

### 1️⃣ حالت پیش‌فرض (Default Scanner)

* اسکن خودکار رنج‌های Cloudflare
* مناسب برای پیدا کردن IP تمیز

### 2️⃣ حالت سفارشی (Custom Scanner)

* وارد کردن IP دلخواه
* مناسب برای تست هدفمند

---

## 🛑 توقف اسکن

در هر زمان می‌توانی اسکن را متوقف کنی:

```
Ctrl + M (Enter)
```

بعد از توقف:

* اسکن متوقف می‌شود
* بهترین IPها پردازش می‌شوند
* کانفیگ‌ها ساخته می‌شوند

---

## 📂 ساختار خروجی

پس از اجرا:

```
D:\allconfigs\
│
├── Configs/
│   ├── Config_1_tls_443_x.x.x.x.json
│   └── ...
│
├── scan_log.txt
├── Verified_IPs.txt
└── sub.txt
```

---

## 📦 فایل‌های تولیدی

### ✅ کانفیگ‌های JSON

* آماده برای کلاینت‌های Xray / v2ray
* بهینه‌شده برای سرعت
* دارای تنظیمات fragment هوشمند

---

### ✅ فایل Verified_IPs.txt

لیست IPهای سالم پیدا شده

---

### ✅ فایل sub.txt

سابسکریپشن base64 آماده ایمپورت در کلاینت‌ها

---

## 🧠 نحوه کار اسکنر

روند کار برنامه:

1. تولید IP تصادفی از رنج Cloudflare
2. تست handshake
3. پیدا کردن پورت باز
4. اندازه‌گیری پینگ (دو بار)
5. تست سرعت واقعی
6. انتخاب بهترین ترکیب
7. ساخت کانفیگ VLESS
8. تولید لینک subscription

---

## ⚡ نکات افزایش سرعت

برای سریع‌تر شدن اسکن:

* مقدار `MAX_WORKERS` را بیشتر کن
* مقدار `SAMPLES_PER_CIDR` را کمتر کن
* لیست DNS را کوتاه نگه دار

---

## 🔐 نکات مهم

* پورت‌های TLS به‌صورت خودکار تشخیص داده می‌شوند
* Fragment برای عبور بهتر بهینه شده
* نتایج بسته به شبکه ممکن است متفاوت باشد

---

## ❗ سلب مسئولیت

این پروژه صرفاً برای اهداف آموزشی و تحقیقاتی منتشر شده است.
مسئولیت استفاده نادرست از آن بر عهده کاربر است.

---

## ⭐ حمایت

اگر پروژه به دردت خورد:

* ⭐ به ریپو ستاره بده
* 🐛 اگر باگ دیدی Issue بزن
* 🔧 Pull Request خوشحال‌کننده است
